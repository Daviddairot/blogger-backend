const config = {
    mongo: {
        Option{},
        url:
    },
    server: {
        host: 'localhost',
        port: 3000,
        // apiPath: '/api',
        // logLevel: 'debug',
        // sessionSecret: 'your-secret-key',
        // sessionMaxAge: 60 * 60 * 1000, // 1 hour
    }
}